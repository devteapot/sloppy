diff --git a/dist/helpers.d.ts b/dist/helpers.d.ts
index 7cc492fdf0371ca4d3b38ce87cd0875790ec2a5b..d80f978b082809b22cf79c5d5e4e8a386d67f14d 100644
--- a/dist/helpers.d.ts
+++ b/dist/helpers.d.ts
@@ -10,6 +10,7 @@ export declare function action<const P extends Record<string, ParamDef>>(params:
     dangerous?: boolean;
     idempotent?: boolean;
     estimate?: "instant" | "fast" | "slow" | "async";
+    resultKind?: string;
 }): Action;
 /** Create an action with options (no params) */
 export declare function action(handler: ActionHandler, options?: {
@@ -18,4 +19,5 @@ export declare function action(handler: ActionHandler, options?: {
     dangerous?: boolean;
     idempotent?: boolean;
     estimate?: "instant" | "fast" | "slow" | "async";
+    resultKind?: string;
 }): Action;
diff --git a/dist/types.d.ts b/dist/types.d.ts
index 43df3521a0f8c74cf2a850e7efc5aaa15400338f..51ce4d185f62d8b783cbd5567c637be56165badc 100644
--- a/dist/types.d.ts
+++ b/dist/types.d.ts
@@ -15,6 +15,7 @@ export interface Affordance {
     dangerous?: boolean;
     idempotent?: boolean;
     estimate?: "instant" | "fast" | "slow" | "async";
+    resultKind?: string;
 }
 export interface NodeMeta {
     summary?: string;
@@ -56,6 +57,7 @@ export type ParamDef = string | {
     description?: string;
     enum?: readonly unknown[];
     items?: JsonSchema;
+    optional?: boolean;
 };
 export type Action = ActionHandler | {
     handler: ActionHandler;
@@ -65,6 +67,7 @@ export type Action = ActionHandler | {
     dangerous?: boolean;
     idempotent?: boolean;
     estimate?: "instant" | "fast" | "slow" | "async";
+    resultKind?: string;
 };
 export interface ContentRef {
     type: "text" | "binary" | "stream";
diff --git a/src/descriptor.ts b/src/descriptor.ts
index ff9ab28f0e50bca743b6af43b2de0e5caeb14620..e402f840665c04305426d2701d416bfac692ad49 100644
--- a/src/descriptor.ts
+++ b/src/descriptor.ts
@@ -154,6 +154,7 @@ function normalizeActions(
         ...(action.dangerous && { dangerous: true }),
         ...(action.idempotent && { idempotent: true }),
         ...(action.estimate && { estimate: action.estimate }),
+        ...(action.resultKind && { resultKind: action.resultKind }),
         ...(action.params && { params: normalizeParams(action.params) }),
       });
     }
@@ -170,6 +171,7 @@ function normalizeParams(params: Record<string, ParamDef>): JsonSchema {
   for (const [key, def] of Object.entries(params)) {
     if (typeof def === "string") {
       properties[key] = { type: def };
+      required.push(key);
     } else {
       properties[key] = {
         type: def.type,
@@ -177,8 +179,8 @@ function normalizeParams(params: Record<string, ParamDef>): JsonSchema {
         ...(def.enum && { enum: def.enum }),
         ...(def.items && { items: def.items }),
       };
+      if (!def.optional) required.push(key);
     }
-    required.push(key);
   }
 
   return { type: "object", properties, required };
diff --git a/src/helpers.ts b/src/helpers.ts
index bfb1294076b2fbf4b76f9b007ad1463c68fd1496..b451913909c0e3071eeb3e79e11e3df9180ab11d 100644
--- a/src/helpers.ts
+++ b/src/helpers.ts
@@ -26,6 +26,7 @@ export function action<const P extends Record<string, ParamDef>>(
     dangerous?: boolean;
     idempotent?: boolean;
     estimate?: "instant" | "fast" | "slow" | "async";
+    resultKind?: string;
   },
 ): Action;
 
@@ -38,6 +39,7 @@ export function action(
     dangerous?: boolean;
     idempotent?: boolean;
     estimate?: "instant" | "fast" | "slow" | "async";
+    resultKind?: string;
   },
 ): Action;
 
diff --git a/src/types.ts b/src/types.ts
index 5e88c227adaaef6bb62daa85eefece2f692e45f8..afac18bab0441ce9d61499d19cbbb338f70eff87 100644
--- a/src/types.ts
+++ b/src/types.ts
@@ -18,6 +18,7 @@ export interface Affordance {
   dangerous?: boolean;
   idempotent?: boolean;
   estimate?: "instant" | "fast" | "slow" | "async";
+  resultKind?: string;
 }
 
 export interface NodeMeta {
@@ -61,7 +62,9 @@ export interface PatchOp {
 
 export type ActionHandler = (params: Record<string, unknown>) => unknown | Promise<unknown>;
 
-export type ParamDef = string | { type: string; description?: string; enum?: readonly unknown[]; items?: JsonSchema };
+export type ParamDef =
+  | string
+  | { type: string; description?: string; enum?: readonly unknown[]; items?: JsonSchema; optional?: boolean };
 
 export type Action =
   | ActionHandler
@@ -73,6 +76,7 @@ export type Action =
       dangerous?: boolean;
       idempotent?: boolean;
       estimate?: "instant" | "fast" | "slow" | "async";
+      resultKind?: string;
     };
 
 export interface ContentRef {
